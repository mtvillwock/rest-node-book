extends ../index

block content

  div.loginbox
    p Start a session
    form(name="login", action="/sessions", method="post")
      input(type="email", placeholder="email", name="email")
      input(type="password", placeholder="password", name="password")
      input(type="submit", value="Login")